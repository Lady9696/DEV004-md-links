const mdLinks = require('../');


describe('mdLinks', () => {

  it('should...', () => {
    console.log('FIX ME!');
  });

});

describe('mdLinks', () => {

  it('deberìa devolver una promesa', () => {
    expect (typeof mdLinks).toBe('promise');
  });

});
